---
phase: 01
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [SITE-03, ID-03, ID-04]
must_haves:
  truths:
    - "Header displays 'Crop Physiology Lab' with COLPOS affiliation eyebrow"
    - "Footer displays 'Colegio de Postgraduados' eyebrow and all 6 nav links"
    - "Google Fonts load via CDN with display=swap and correct weights"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Sticky header with rebranded eyebrow and active nav highlighting"
    - path: "src/components/Footer.astro"
      provides: "3-column footer with COLPOS branding and updated nav links"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Font loading via Google Fonts CDN with preconnect"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "Google Fonts CDN"
      via: "<link rel='stylesheet'> and <link rel='preconnect'> tags"
      pattern: "fonts.googleapis.com"
---

<objective>
Update shared components (Header, Footer, BaseLayout) to reflect the rebrand and load web fonts. Header shows COLPOS affiliation and active page highlighting works with new navigation. Footer shows COLPOS branding and updated navigation links. BaseLayout loads Google Fonts CDN.

Purpose: Ensure all pages share the correct branding, navigation, and typography.
</objective>

<execution_context>
@AGENTS.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md

Design system is LOCKED:
- tailwind.config.js: colors, fonts, shadows (DO NOT MODIFY)
- src/index.css: semantic utilities (DO NOT MODIFY)

Key decisions:
- D-04: Header eyebrow "Plant Physiology Research" → "COLPOS | Crop Physiology Research"
- D-05: COLPOS affiliation displayed alongside lab name
- D-08: Footer eyebrow "Institutional Research Group" → "Colegio de Postgraduados"
- D-09: Google Fonts CDN with display=swap
- D-10: Weights: Playfair Display 400,500,600,700; Source Sans 3 400,600,700; IBM Plex Mono 400
- D-14: BaseLayout wraps all pages with Header + Footer
- D-15: Header pattern (sticky, backdrop-blur, <details> mobile menu) is locked
- D-16: Footer 3-column grid pattern is locked
</context>

<tasks>

<task type="execute">
  <name>Task 1: Update Header.astro with COLPOS rebrand</name>
  <files>src/components/Header.astro</files>
  <read_first>
    src/components/Header.astro
    src/data/site.json (updated by Plan 01-01)
    .planning/phases/01-foundation/01-CONTEXT.md (D-04, D-05, D-15 decisions)
  </read_first>
  <action>
    Update src/components/Header.astro with these concrete changes:

    1. Replace the hardcoded eyebrow text on line 17:
       OLD: <p class="eyebrow mb-1">Plant Physiology Research</p>
       NEW: <p class="eyebrow mb-1">COLPOS | Crop Physiology Research</p>

    2. Verify the lab name still reads from site.json (line 18):
       <div class="font-serif text-xl font-semibold text-charcoal sm:text-2xl">{site.labName}</div>
       This should now render "Crop Physiology Lab" because site.json was updated in Plan 01-01.

    3. Ensure the aria-label on the home link (line 16) uses site.labName:
       aria-label={`${site.labName} home`}

    4. Verify the active page highlighting logic (lines 23-38) still works correctly with the new navigation items. The logic uses navigation.main which is now updated — no code changes needed, just verify the pattern works.

    5. Keep all existing styling: sticky top-0, backdrop-blur, min-h-[5rem], page-shell container, <details> mobile menu, group-open hamburger animation.

    6. Ensure mobile nav aria-labels are correct: "Toggle navigation menu" on <summary>, "Primary" on desktop nav, "Mobile" on mobile nav.
  </action>
  <acceptance_criteria>
    - grep -c "Plant Physiology Research" src/components/Header.astro returns 0
    - grep -c "COLPOS | Crop Physiology Research" src/components/Header.astro returns >= 1
    - grep -c "site.labName" src/components/Header.astro returns >= 1
    - grep -c "sticky top-0" src/components/Header.astro returns >= 1
    - grep -c "details" src/components/Header.astro returns >= 1
    - grep -c "aria-label=\"Toggle navigation menu\"" src/components/Header.astro returns >= 1
    - grep -c "aria-label=\"Primary\"" src/components/Header.astro returns >= 1
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Update Footer.astro with COLPOS rebrand and new nav</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    src/components/Footer.astro
    src/data/navigation.json (updated by Plan 01-01)
    src/data/site.json (updated by Plan 01-01)
    .planning/phases/01-foundation/01-CONTEXT.md (D-08, D-16 decisions)
  </read_first>
  <action>
    Update src/components/Footer.astro with these concrete changes:

    1. Replace the hardcoded eyebrow text on line 10:
       OLD: <p class="eyebrow mb-4">Institutional Research Group</p>
       NEW: <p class="eyebrow mb-4">Colegio de Postgraduados</p>

    2. Verify site.labName renders correctly on line 11:
       <h2 class="text-2xl font-semibold">{site.labName}</h2>
       Should now display "Crop Physiology Lab".

    3. Verify site.mission renders correctly on line 12 and reflects COLPOS context (via updated site.json).

    4. Verify the footer nav list (lines 17-29) uses navigation.footer which now has 6 items per the updated navigation.json. No code changes needed since it iterates with .map().

    5. Verify the contact info section (lines 35-41) displays:
       - site.institution (now "Colegio de Postgraduados (COLPOS), Montecillo, Mexico")
       - site.location (now COLPOS address)
       - site.email (now cropphysiology@colpos.mx)

    6. Keep all existing styling: border-t border-line, bg-[#f2efe9], page-shell container, lg:grid-cols-[1.2fr_0.8fr_0.8fr], eyebrow class.
  </action>
  <acceptance_criteria>
    - grep -c "Institutional Research Group" src/components/Footer.astro returns 0
    - grep -c "Colegio de Postgraduados" src/components/Footer.astro returns >= 1
    - grep -c "navigation.footer" src/components/Footer.astro returns >= 1
    - grep -c "site.institution" src/components/Footer.astro returns >= 1
    - grep -c "site.location" src/components/Footer.astro returns >= 1
    - grep -c "site.email" src/components/Footer.astro returns >= 1
    - grep -c "lg:grid-cols-\[1.2fr_0.8fr_0.8fr\]" src/components/Footer.astro returns >= 1
    - grep -c "bg-\[#f2efe9\]" src/components/Footer.astro returns >= 1
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Add Google Fonts CDN to BaseLayout.astro</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    src/layouts/BaseLayout.astro
    .planning/phases/01-foundation/01-CONTEXT.md (D-09, D-10 decisions)
  </read_first>
  <action>
    Update src/layouts/BaseLayout.astro to load Google Fonts via CDN with the following concrete changes in the <head> section (after the existing <meta> tags, before <link rel="icon">):

    Add these three <link> tags in order:

    1. Preconnect to fonts.googleapis.com:
       <link rel="preconnect" href="https://fonts.googleapis.com" />

    2. Preconnect to fonts.gstatic.com with crossorigin:
       <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    3. Google Fonts stylesheet loading Playfair Display (400,500,600,700), Source Sans 3 (400,600,700), and IBM Plex Mono (400) with display=swap:
       <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400&family=Playfair+Display:wght@400;500;600;700&family=Source+Sans+3:wght@400;600;700&display=swap" rel="stylesheet" />

    Place these links BEFORE the existing favicon link. Ensure the <link rel="icon"> tag remains AFTER the fonts link.

    Do NOT modify any other part of BaseLayout. Keep the existing Props interface, title/description meta tags, og: tags, and HTML structure exactly as-is.
  </action>
  <acceptance_criteria>
    - grep -c "fonts.googleapis.com" src/layouts/BaseLayout.astro returns >= 2
    - grep -c "fonts.gstatic.com" src/layouts/BaseLayout.astro returns >= 1
    - grep -c "Playfair+Display" src/layouts/BaseLayout.astro returns >= 1
    - grep -c "Source+Sans+3" src/layouts/BaseLayout.astro returns >= 1
    - grep -c "IBM+Plex+Mono" src/layouts/BaseLayout.astro returns >= 1
    - grep -c "display=swap" src/layouts/BaseLayout.astro returns >= 1
    - grep -c "preconnect" src/layouts/BaseLayout.astro returns >= 2
    - The <link rel="icon"> tag must appear AFTER the Google Fonts link in the file
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Verify semantic utilities and design system are preserved</name>
  <files>
    src/index.css
    tailwind.config.js
  </files>
  <read_first>
    src/index.css
    tailwind.config.js
    .planning/phases/01-foundation/01-CONTEXT.md (D-11, D-12, D-13 decisions)
  </read_first>
  <action>
    Verify that the design system files have NOT been modified and remain locked:

    1. Check src/index.css contains all expected semantic utilities:
       - .page-shell
       - .section-space
       - .eyebrow
       - .surface-card
       - .page-divider
       - .button-primary, .button-secondary, .button-tertiary
       - Focus-visible styles with ring-pine
       - Body bg-parchment with gradient overlay

    2. Check tailwind.config.js contains expected colors:
       - parchment, surface, ink, muted, line, pine, sage, earth, soft, charcoal
       - Font families: Playfair Display, Source Sans 3, IBM Plex Mono
       - Box shadow: card
       - Max width: content

    3. Confirm NO modifications were made to these files. If any changes are detected, revert them.
  </action>
  <acceptance_criteria>
    - grep -c "\.page-shell" src/index.css returns >= 1
    - grep -c "\.surface-card" src/index.css returns >= 1
    - grep -c "ring-pine" src/index.css returns >= 1
    - grep -c "bg-parchment" src/index.css returns >= 1
    - grep -c "parchment:" tailwind.config.js returns >= 1
    - grep -c "Playfair Display" tailwind.config.js returns >= 1
    - grep -c "Source Sans 3" tailwind.config.js returns >= 1
    - grep -c "IBM Plex Mono" tailwind.config.js returns >= 1
    - MD5 hash of tailwind.config.js matches the original (or manual diff shows no changes)
    - MD5 hash of src/index.css matches the original (or manual diff shows no changes)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 5: Verify mobile navigation patterns are intact</name>
  <files>src/components/Header.astro</files>
  <read_first>
    src/components/Header.astro
    .planning/phases/01-foundation/01-CONTEXT.md (D-15, D-22 decisions)
  </read_first>
  <action>
    Verify the Header.astro mobile navigation implementation is preserved and will work with 8 nav items:

    1. Confirm the mobile menu uses a <details> element (not JavaScript).
    2. Confirm the <summary> has the class "group" and the hamburger icon uses "group-open:" classes for animation.
    3. Confirm the mobile nav container uses "group-open:max-h-[32rem]" which provides enough height for 8 navigation items.
    4. Confirm there is an aria-label="Toggle navigation menu" on the <summary>.
    5. Confirm there is an aria-label="Mobile" on the mobile <nav> element.
    6. Verify the container uses "page-shell" class for consistent padding.

    No code changes needed if all patterns are intact — this is a verification task.
  </action>
  <acceptance_criteria>
    - grep -c "<details" src/components/Header.astro returns >= 1
    - grep -c "group-open:max-h-\[32rem\]" src/components/Header.astro returns >= 1
    - grep -c "aria-label=\"Toggle navigation menu\"" src/components/Header.astro returns >= 1
    - grep -c "aria-label=\"Mobile\"" src/components/Header.astro returns >= 1
    - grep -c "page-shell" src/components/Header.astro returns >= 1
    - grep -c "group-open:" src/components/Header.astro returns >= 3
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→static HTML | No runtime trust boundary; all content is static |
| client→Google Fonts CDN | External resource loaded by browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Google Fonts CDN | accept | No PII sent; standard web font loading |
| T-02-02 | Denial of Service | Google Fonts CDN dependency | accept | Fonts have system fallbacks (Georgia, system-ui, ui-monospace) |
| T-02-03 | Tampering | Design system files | mitigate | Verify no changes to tailwind.config.js and index.css |
</threat_model>

<verification>
- Header renders "COLPOS | Crop Physiology Research" eyebrow
- Footer renders "Colegio de Postgraduados" eyebrow and 6 nav links
- Google Fonts <link> tags present in BaseLayout <head> with preconnect
- Design system files unchanged (tailwind.config.js, index.css)
- Mobile menu <details> pattern intact with sufficient max-height
</verification>

<success_criteria>
- Header eyebrow reads "COLPOS | Crop Physiology Research" (not "Plant Physiology Research")
- Footer eyebrow reads "Colegio de Postgraduados" (not "Institutional Research Group")
- BaseLayout contains Google Fonts CDN link with display=swap and correct weights
- No modifications to tailwind.config.js or src/index.css
- Mobile navigation uses <details> element with group-open animation
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
