---
phase: 01
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/content/research/README.md
  - src/content/team/README.md
  - src/content/capabilities/README.md
  - src/content/protocols/README.md
  - src/content/students/README.md
  - src/pages/capabilities.astro
  - src/pages/methods.astro
autonomous: true
requirements: [SITE-07, ID-02, SITE-01]
must_haves:
  truths:
    - "src/content/ directory exists with 5 subdirectories for Markdown content"
    - "Capabilities placeholder page renders at /capabilities with BaseLayout and PageHero"
    - "Methods & Protocols placeholder page renders at /methods with BaseLayout and PageHero"
    - "Content directories contain only standard Markdown/README files (no Astro-specific features)"
  artifacts:
    - path: "src/content/research/README.md"
      provides: "Research theme content documentation"
    - path: "src/content/team/README.md"
      provides: "Team member content documentation"
    - path: "src/content/capabilities/README.md"
      provides: "Capabilities content documentation"
    - path: "src/content/protocols/README.md"
      provides: "Methods & protocols content documentation"
    - path: "src/content/students/README.md"
      provides: "Student recruitment content documentation"
    - path: "src/pages/capabilities.astro"
      provides: "Placeholder Capabilities page"
    - path: "src/pages/methods.astro"
      provides: "Placeholder Methods & Protocols page"
  key_links:
    - from: "src/pages/capabilities.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import BaseLayout from '../layouts/BaseLayout.astro'"
      pattern: "import BaseLayout"
---

<objective>
Create the content directory infrastructure for portable Markdown content and add placeholder Astro pages for the two new routes: Capabilities and Methods & Protocols. Establish the pattern that content lives in standard Markdown/JSON files, not Astro-specific features.

Purpose: Enable Phase 2-4 content population while ensuring navigation links to all 8 pages resolve to valid pages.
</objective>

<execution_context>
@AGENTS.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md

Key decisions to implement:
- D-17: Create src/content/ with subdirectories: research/, team/, capabilities/, protocols/, students/
- D-18: Keep src/data/ for JSON files
- D-19: NO Astro Content Collections — use standard file I/O
- D-20: Create placeholder .astro stubs for Capabilities, Methods & Protocols pages
- D-07: Methods nav label is "Methods" (abbreviated), page title is "Methods & Protocols" (full)
</context>

<tasks>

<task type="execute">
  <name>Task 1: Create content directory structure with README files</name>
  <files>
    src/content/research/README.md
    src/content/team/README.md
    src/content/capabilities/README.md
    src/content/protocols/README.md
    src/content/students/README.md
  </files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md (D-17, D-18, D-19 decisions)
    AGENTS.md (content portability requirements)
  </read_first>
  <action>
    Create the following directory structure under src/content/ with a README.md in each directory:

    1. src/content/research/README.md — Content:
       # Research Themes
       
       This directory contains Markdown files for each research theme.
       Each file should have YAML frontmatter with fields: title, summary, methods, tools, outputs.
       These files will be read using standard file I/O (not Astro Content Collections).
       
       Expected files (Phase 2):
       - theme-01.md, theme-02.md, theme-03.md, theme-04.md, theme-05.md

    2. src/content/team/README.md — Content:
       # Team Members
       
       This directory contains Markdown files for the PI and each team member.
       Each file should have YAML frontmatter with fields: name, role, interests, bio, email.
       Supplements the structured data in src/data/team.json.
       
       Expected files (Phase 2):
       - pi.md, member-*.md

    3. src/content/capabilities/README.md — Content:
       # Capabilities and Equipment
       
       This directory contains Markdown files describing lab equipment and analytical services.
       Each file should have YAML frontmatter with fields: title, category, equipment, description, contact.
       
       Expected files (Phase 3):
       - capability-*.md

    4. src/content/protocols/README.md — Content:
       # Methods & Protocols
       
       This directory contains Markdown files for standard operating procedures (SOPs).
       Each file should have YAML frontmatter with fields: title, category, version, description, equipment, duration.
       Replaces the old Resources JSON data structure.
       
       Expected files (Phase 3):
       - protocol-*.md

    5. src/content/students/README.md — Content:
       # Student Recruitment
       
       This directory contains Markdown files for student recruitment content and FAQ.
       Each file should have YAML frontmatter with fields: title, type (recruitment/faq), content.
       
       Expected files (Phase 4):
       - recruitment.md, faq.md

    Each README must explicitly state: "Content is stored in standard Markdown format for portability. Do not use Astro Content Collections."
  </action>
  <acceptance_criteria>
    - ls src/content/research/README.md exists and is a file
    - ls src/content/team/README.md exists and is a file
    - ls src/content/capabilities/README.md exists and is a file
    - ls src/content/protocols/README.md exists and is a file
    - ls src/content/students/README.md exists and is a file
    - grep -r "Content Collections" src/content/ returns empty
    - grep -c "Do not use Astro Content Collections" src/content/research/README.md returns >= 1
    - grep -c "Do not use Astro Content Collections" src/content/protocols/README.md returns >= 1
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Create Capabilities placeholder page</name>
  <files>src/pages/capabilities.astro</files>
  <read_first>
    src/layouts/BaseLayout.astro
    src/components/PageHero.astro
    src/components/CTASection.astro
    .planning/phases/01-foundation/01-CONTEXT.md (D-20 decision)
  </read_first>
  <action>
    Create src/pages/capabilities.astro with the following content. Use BaseLayout, PageHero, and CTASection components. The page should use the lab name from site.json dynamically.

    ---
    import BaseLayout from '../layouts/BaseLayout.astro';
    import PageHero from '../components/PageHero.astro';
    import CTASection from '../components/CTASection.astro';
    import site from '../data/site.json';
    ---

    <BaseLayout
      title={`${site.labName} | Capabilities`}
      description="Lab equipment, analytical services, and collaboration opportunities at the Crop Physiology Lab."
    >
      <PageHero
        eyebrow="Capabilities"
        title="Equipment and analytical services"
        description="An overview of the instrumentation, experimental platforms, and analytical workflows available for collaboration. Detailed capability profiles will be added in the next phase."
      >
        <div class="space-y-4">
          <p class="font-mono text-xs uppercase tracking-[0.22em] text-muted">Status</p>
          <p>Detailed equipment specifications and service descriptions are coming soon.</p>
        </div>
      </PageHero>

      <section class="section-space">
        <div class="page-shell">
          <div class="max-w-3xl">
            <p class="eyebrow mb-3">Coming Soon</p>
            <h2 class="text-3xl font-semibold leading-tight sm:text-4xl">Capability profiles under development</h2>
            <p class="mt-4">
              This page will list equipment, sample requirements, and collaboration pathways.
              Content will be editable via Markdown files in src/content/capabilities/.
            </p>
          </div>
          <div class="mt-10 grid gap-6 md:grid-cols-2">
            <article class="surface-card p-6">
              <p class="eyebrow mb-3">Analytical</p>
              <h3 class="text-xl font-semibold">Chromatography and metabolite analysis</h3>
              <p class="mt-4">Protocols for HPLC-based metabolite profiling and small molecule quantification.</p>
            </article>
            <article class="surface-card p-6">
              <p class="eyebrow mb-3">Phenotyping</p>
              <h3 class="text-xl font-semibold">Controlled-environment phenotyping</h3>
              <p class="mt-4">Growth chamber and greenhouse-based trait measurement under controlled conditions.</p>
            </article>
          </div>
        </div>
      </section>

      <CTASection
        eyebrow="Collaboration"
        title="Interested in using lab equipment or services?"
        description="We welcome collaborations with researchers at COLPOS and beyond. Contact us to discuss your project needs."
        actions={[
          { label: 'Contact the Lab', href: '/contact', variant: 'primary' },
          { label: 'View Research', href: '/research', variant: 'secondary' },
        ]}
      />
    </BaseLayout>

    Do NOT use ``` code blocks in the action — this is prose description. The executor should create the file based on the description above.
  </action>
  <acceptance_criteria>
    - ls src/pages/capabilities.astro exists and is a file
    - grep -c "import BaseLayout" src/pages/capabilities.astro returns >= 1
    - grep -c "import PageHero" src/pages/capabilities.astro returns >= 1
    - grep -c "Capabilities" src/pages/capabilities.astro returns >= 1
    - grep -c "site.labName" src/pages/capabilities.astro returns >= 1
    - grep -c "/contact" src/pages/capabilities.astro returns >= 1
    - grep -c "/research" src/pages/capabilities.astro returns >= 1
    - grep -c "surface-card" src/pages/capabilities.astro returns >= 1
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Create Methods & Protocols placeholder page</name>
  <files>src/pages/methods.astro</files>
  <read_first>
    src/layouts/BaseLayout.astro
    src/components/PageHero.astro
    src/components/CTASection.astro
    .planning/phases/01-foundation/01-CONTEXT.md (D-20, D-07 decisions)
  </read_first>
  <action>
    Create src/pages/methods.astro with the following content. Use BaseLayout, PageHero, and CTASection components. The page title must use the FULL label "Methods & Protocols" (not the abbreviated "Methods").

    ---
    import BaseLayout from '../layouts/BaseLayout.astro';
    import PageHero from '../components/PageHero.astro';
    import CTASection from '../components/CTASection.astro';
    import site from '../data/site.json';
    ---

    <BaseLayout
      title={`${site.labName} | Methods & Protocols`}
      description="Standard operating procedures, analytical protocols, and documented lab workflows from the Crop Physiology Lab."
    >
      <PageHero
        eyebrow="Methods & Protocols"
        title="Standard operating procedures and documented workflows"
        description="Searchable protocols for analytical chemistry, experimental design, phenotyping, and data management. Detailed procedures will be added in the next phase."
      >
        <div class="space-y-4">
          <p class="font-mono text-xs uppercase tracking-[0.22em] text-muted">Status</p>
          <p>Protocol documentation is being migrated from the previous resources library.</p>
        </div>
      </PageHero>

      <section class="section-space">
        <div class="page-shell">
          <div class="max-w-3xl">
            <p class="eyebrow mb-3">Coming Soon</p>
            <h2 class="text-3xl font-semibold leading-tight sm:text-4xl">Protocol documentation under development</h2>
            <p class="mt-4">
              This page will host searchable protocols with category filtering and search.
              Content will be editable via Markdown files in src/content/protocols/.
            </p>
          </div>
          <div class="mt-10 grid gap-6 md:grid-cols-2">
            <article class="surface-card p-6">
              <p class="eyebrow mb-3">Analytical</p>
              <h3 class="text-xl font-semibold">HPLC metabolite profiling</h3>
              <p class="mt-4">Sample preparation, extraction, chromatographic conditions, and data processing for targeted metabolite analysis.</p>
            </article>
            <article class="surface-card p-6">
              <p class="eyebrow mb-3">Experimental</p>
              <h3 class="text-xl font-semibold">Growth chamber experimental design</h3>
              <p class="mt-4">Standard layouts, replication strategies, randomization, and environmental control protocols.</p>
            </article>
          </div>
        </div>
      </section>

      <CTASection
        eyebrow="Resources"
        title="Looking for a specific protocol?"
        description="Contact us if you need a protocol that is not yet listed. We are actively documenting and standardizing our core procedures."
        actions={[
          { label: 'Contact the Lab', href: '/contact', variant: 'primary' },
          { label: 'View Capabilities', href: '/capabilities', variant: 'secondary' },
        ]}
      />
    </BaseLayout>

    The page URL is /methods (matching the abbreviated nav label), but the page title and h1 use "Methods & Protocols".
  </action>
  <acceptance_criteria>
    - ls src/pages/methods.astro exists and is a file
    - grep -c "import BaseLayout" src/pages/methods.astro returns >= 1
    - grep -c "import PageHero" src/pages/methods.astro returns >= 1
    - grep -c "Methods & Protocols" src/pages/methods.astro returns >= 2
    - grep -c "site.labName" src/pages/methods.astro returns >= 1
    - grep -c "/contact" src/pages/methods.astro returns >= 1
    - grep -c "/capabilities" src/pages/methods.astro returns >= 1
    - grep -c "surface-card" src/pages/methods.astro returns >= 1
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Verify content portability rules</name>
  <files>src/content/</files>
  <read_first>
    AGENTS.md (content portability requirements)
    .planning/phases/01-foundation/01-CONTEXT.md (D-19 decision)
  </read_first>
  <action>
    Verify that the content directory follows portability rules:

    1. Confirm src/content/ contains ONLY .md and README.md files.
    2. Confirm there is NO src/content/config.ts or src/content/config.js file.
    3. Confirm there are NO .astro files in any subdirectory of src/content/.
    4. Confirm all README files explicitly state that content is in standard Markdown format.
    5. Verify that the directory names (research, team, capabilities, protocols, students) are self-describing and follow kebab-case convention.

    This ensures content can migrate to Hugo, Jekyll, 11ty, or any other static site generator without rewriting.
  </action>
  <acceptance_criteria>
    - find src/content/ -name "*.astro" returns empty
    - find src/content/ -name "config.ts" returns empty
    - find src/content/ -name "config.js" returns empty
    - find src/content/ -type f | grep -v "README.md" | grep -v ".md" returns empty
    - grep -r "standard Markdown" src/content/ returns >= 5 matches
    - ls src/content/ | grep -c "research" returns 1
    - ls src/content/ | grep -c "team" returns 1
    - ls src/content/ | grep -c "capabilities" returns 1
    - ls src/content/ | grep -c "protocols" returns 1
    - ls src/content/ | grep -c "students" returns 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content→build | Markdown files are processed at build time only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | README content paths | accept | Directory structure is public by design |
| T-03-02 | Tampering | Markdown content | accept | Content versioned in git; no runtime mutation |
</threat_model>

<verification>
- src/content/ exists with 5 subdirectories
- Each subdirectory contains a README.md explaining content format
- No Astro-specific files (.astro, config.ts) in src/content/
- src/pages/capabilities.astro exists and uses BaseLayout + PageHero
- src/pages/methods.astro exists and uses BaseLayout + PageHero with "Methods & Protocols" title
</verification>

<success_criteria>
- src/content/ directory has subdirectories: research/, team/, capabilities/, protocols/, students/
- Each subdirectory contains a README.md with content format documentation
- No Astro Content Collections configuration or .astro files in src/content/
- src/pages/capabilities.astro renders with BaseLayout, PageHero, and placeholder content
- src/pages/methods.astro renders with BaseLayout, PageHero, and "Methods & Protocols" title
- Both placeholder pages include CTASection with links to /contact
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
