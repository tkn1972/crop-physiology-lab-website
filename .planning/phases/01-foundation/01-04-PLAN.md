---
phase: 01
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - .planning/UPDATE_WORKFLOW.md
autonomous: false
requirements: [SITE-01, SITE-02, SITE-08]
must_haves:
  truths:
    - "npm run build succeeds with zero errors and zero warnings"
    - "All 8 pages produce valid HTML in dist/"
    - "Update workflow is documented in .planning/UPDATE_WORKFLOW.md"
  artifacts:
    - path: "dist/"
      provides: "Static HTML build output with all 8 pages"
    - path: ".planning/UPDATE_WORKFLOW.md"
      provides: "Content update and deployment documentation"
  key_links:
    - from: "src/pages/*.astro"
      to: "dist/*.html"
      via: "Astro static build (npm run build)"
      pattern: "npm run build"
---

<objective>
Verify the entire site builds successfully, all 8 pages render with correct navigation, fonts load properly, and document the content update workflow for future maintainers.

Purpose: Ensure Phase 1 deliverables are production-ready and that a non-technical maintainer can update content.
</objective>

<execution_context>
@AGENTS.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 1 success criteria)
@.planning/phases/01-foundation/01-CONTEXT.md (D-24, D-25 decisions)

Phase 1 Success Criteria from ROADMAP:
1. Site builds without errors (npm run build succeeds)
2. All shared components render correctly
3. Navigation works with active state highlighting on all 8 pages
4. Rebrand complete — no references to old name remain
5. Fonts load correctly
6. Content directory structure is clean and documented
7. Mobile menu opens and closes correctly
8. Sticky header stays fixed on scroll
</context>

<tasks>

<task type="execute">
  <name>Task 1: Verify static build passes with zero errors</name>
  <files>
    package.json
    astro.config.mjs
  </files>
  <read_first>
    package.json
    astro.config.mjs
    .gitignore
  </read_first>
  <action>
    Run the Astro build and verify it completes successfully:

    1. Execute: npm run build
    2. Verify the command exits with code 0 (success).
    3. Verify there are NO error messages in the output.
    4. Verify there are NO warning messages in the output (or note any warnings for remediation).
    5. Verify dist/ directory is created and contains HTML files.
    6. Verify dist/ is listed in .gitignore (should already be there per existing .gitignore).
    7. Check that astro.config.mjs is properly configured for static output (it should already have export default with integrations: [tailwind()]).

    If the build fails, read the error output, fix the root cause (likely a missing import, syntax error, or broken reference), and retry until it passes.
  </action>
  <acceptance_criteria>
    - npm run build exits with code 0
    - ls dist/ exists and contains files
    - ls dist/index.html exists
    - grep -c "dist/" .gitignore returns >= 1
    - No ERROR-level messages in build output
    - No WARNING-level messages in build output (or documented and accepted)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Verify all 8 pages produce HTML output</name>
  <files>dist/</files>
  <read_first>
    src/data/navigation.json (verify expected routes)
  </read_first>
  <action>
    Verify that the build output contains HTML files for all 8 pages:

    1. Check dist/index.html exists (Home)
    2. Check dist/research/index.html exists (Research)
    3. Check dist/capabilities/index.html exists (Capabilities)
    4. Check dist/methods/index.html exists (Methods & Protocols)
    5. Check dist/team/index.html exists (Team)
    6. Check dist/publications/index.html exists (Publications)
    7. Check dist/students/index.html exists (Students)
    8. Check dist/contact/index.html exists (Contact)

    Also verify that each HTML file contains the expected page title in the <title> tag (e.g., "Crop Physiology Lab | Capabilities").

    Check that the old Resources page (dist/resources/index.html) still exists if src/pages/resources.astro was not removed — this is expected since Phase 3 will handle deprecation.
  </action>
  <acceptance_criteria>
    - ls dist/index.html exists
    - ls dist/research/index.html exists
    - ls dist/capabilities/index.html exists
    - ls dist/methods/index.html exists
    - ls dist/team/index.html exists
    - ls dist/publications/index.html exists
    - ls dist/students/index.html exists
    - ls dist/contact/index.html exists
    - grep -c "Crop Physiology Lab | Capabilities" dist/capabilities/index.html returns >= 1
    - grep -c "Methods & Protocols" dist/methods/index.html returns >= 1
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Verify Google Fonts links in HTML output</name>
  <files>dist/index.html</files>
  <read_first>dist/index.html</read_first>
  <action>
    Verify that the built HTML includes the Google Fonts links:

    1. Check dist/index.html for the presence of fonts.googleapis.com links.
    2. Verify the link includes "display=swap".
    3. Verify the link includes "Playfair+Display".
    4. Verify the link includes "Source+Sans+3".
    5. Verify the link includes "IBM+Plex+Mono".
    6. Verify preconnect links exist for fonts.googleapis.com and fonts.gstatic.com.

    Check one other page (e.g., dist/capabilities/index.html) to confirm BaseLayout is applied consistently.
  </action>
  <acceptance_criteria>
    - grep -c "fonts.googleapis.com" dist/index.html returns >= 1
    - grep -c "display=swap" dist/index.html returns >= 1
    - grep -c "Playfair+Display" dist/index.html returns >= 1
    - grep -c "Source+Sans+3" dist/index.html returns >= 1
    - grep -c "IBM+Plex+Mono" dist/index.html returns >= 1
    - grep -c "fonts.gstatic.com" dist/index.html returns >= 1
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Document content update workflow</name>
  <files>.planning/UPDATE_WORKFLOW.md</files>
  <read_first>
    .planning/PROJECT.md (content format requirements)
    AGENTS.md (content portability guidelines)
  </read_first>
  <action>
    Create .planning/UPDATE_WORKFLOW.md with the following concrete content. The document must explain how a non-technical maintainer (PI or grad student) can update site content and deploy changes.

    ---
    # Content Update Workflow

    ## Overview

    All content on the Crop Physiology Lab website is stored in standard files (Markdown and JSON) that can be edited with any text editor. No CMS, database, or special software is required.

    ## Where Content Lives

    ### JSON Data Files (src/data/)

    These files contain structured data that components read and display:

    - **site.json** — Lab name, tagline, mission, contact info, hero text, PI details
    - **navigation.json** — Navigation links for header and footer
    - **publications.json** — Publication list (title, authors, journal, year, DOI)
    - **team.json** — Team members (name, role, photo, bio, interests)
    - **research.json** — Research themes (title, summary, methods, tools)
    - **students.json** — Student onboarding info, expectations, FAQ

    ### Markdown Content Files (src/content/)

    These directories hold content for pages built in Phases 2-4:

    - **src/content/research/** — Research theme Markdown files (Phase 2)
    - **src/content/team/** — Team member Markdown files (Phase 2)
    - **src/content/capabilities/** — Equipment and service Markdown files (Phase 3)
    - **src/content/protocols/** — Protocol and SOP Markdown files (Phase 3)
    - **src/content/students/** — Recruitment and FAQ Markdown files (Phase 4)

    Each Markdown file should include YAML frontmatter at the top:

    ```yaml
    ---
    title: "Your Title Here"
    summary: "Brief description"
    ---
    ```

    ## How to Edit Content

    1. Open the relevant file in your text editor
    2. Make your changes
    3. Save the file
    4. Run `npm run build` locally to preview (or skip if you trust the change)
    5. Commit the change with git
    6. Push to the main branch — deployment happens automatically

    ## How to Deploy

    The site is built automatically when you push to the main branch:

    ```bash
    git add .
    git commit -m "content: update [description]"
    git push origin main
    ```

    Netlify/Vercel will run `npm run build` and deploy the `dist/` folder.

    ## Important Rules

    - Do NOT edit .astro files unless you are changing page structure
    - Do NOT use Astro Content Collections — keep content in Markdown/JSON
    - Do NOT modify tailwind.config.js or src/index.css — the design system is locked
    - Always validate JSON files after editing (no trailing commas)
    - Always include YAML frontmatter in Markdown files
    - Images should be placed in public/images/ and referenced with /images/... paths

    ## Need Help?

    Contact the person who set up the site or refer to AGENTS.md for technical details.
    ---

    Save this as .planning/UPDATE_WORKFLOW.md.
  </action>
  <acceptance_criteria>
    - ls .planning/UPDATE_WORKFLOW.md exists and is a file
    - grep -c "src/data/" .planning/UPDATE_WORKFLOW.md returns >= 1
    - grep -c "src/content/" .planning/UPDATE_WORKFLOW.md returns >= 1
    - grep -c "npm run build" .planning/UPDATE_WORKFLOW.md returns >= 1
    - grep -c "git push" .planning/UPDATE_WORKFLOW.md returns >= 1
    - grep -c "Do NOT use Astro Content Collections" .planning/UPDATE_WORKFLOW.md returns >= 1
    - grep -c "Do NOT modify tailwind.config.js" .planning/UPDATE_WORKFLOW.md returns >= 1
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual verification of responsive behavior and navigation</name>
  <what-built>
    All 8 pages are built and navigation links are updated. The site uses Google Fonts, sticky header, <details>-based mobile menu, and responsive Tailwind breakpoints.
  </what-built>
  <how-to-verify>
    1. Start a local preview server: `npm run preview` (or open dist/index.html in browser)
    2. Navigate to each of the 8 pages via the header navigation:
       - Home (/) — Verify "Crop Physiology Lab" appears in header
       - Research (/research) — Verify nav item "Research" is highlighted
       - Capabilities (/capabilities) — Verify page loads with "Capabilities" heading
       - Methods (/methods) — Verify page loads with "Methods & Protocols" heading
       - Team (/team) — Verify nav item "Team" is highlighted
       - Publications (/publications) — Verify nav item "Publications" is highlighted
       - Students (/students) — Verify nav item "Students" is highlighted
       - Contact (/contact) — Verify nav item "Contact" is highlighted
    3. Test mobile menu:
       - Resize browser to < 1024px width (or use dev tools mobile view)
       - Click the hamburger icon (top right)
       - Verify menu opens showing all 8 links
       - Click a link — verify page navigates and menu closes
    4. Test sticky header:
       - Scroll down any page
       - Verify header stays fixed at top with backdrop blur
    5. Test font loading:
       - Open browser DevTools → Network tab
       - Reload page
       - Verify requests to fonts.googleapis.com and fonts.gstatic.com appear
       - Verify Playfair Display, Source Sans 3, and IBM Plex Mono are loaded
    6. Test responsive layout:
       - Test at 375px width (mobile): verify no horizontal scroll, content reflows
       - Test at 768px width (tablet): verify grid columns adjust
       - Test at 1440px width (desktop): verify layout looks correct
  </how-to-verify>
  <resume-signal>Type "approved" when all visual checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build→dist | Build output is static HTML, no runtime trust boundary |
| maintainer→git | Content changes tracked in version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | dist/ output contains emails | accept | Public contact info by design |
| T-04-02 | Denial of Service | Build dependency on npm packages | accept | Standard npm project with package-lock.json |
| T-04-03 | Tampering | UPDATE_WORKFLOW.md instructions | accept | Maintainers follow git workflow; changes are auditable |
</threat_model>

<verification>
- npm run build succeeds with code 0
- dist/ contains HTML for all 8 pages
- Google Fonts links present in built HTML
- .planning/UPDATE_WORKFLOW.md exists and documents the update process
- Visual verification confirms navigation, mobile menu, sticky header, and fonts
</verification>

<success_criteria>
- npm run build exits with code 0 and produces dist/ output
- All 8 pages have corresponding HTML files in dist/
- Google Fonts are loaded (visible in browser Network tab)
- Navigation highlights active page correctly on all 8 pages
- Mobile menu opens/closes with animated hamburger icon
- Sticky header remains fixed on scroll
- .planning/UPDATE_WORKFLOW.md documents editing and deployment steps
- No errors or warnings in build output
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
