---
context: phase
phase: 02-core-content
task: null
total_tasks: null
status: paused
last_updated: 2026-05-23T01:00:24Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Content portability — All content must stay in standard Markdown + JSON, not Astro Content Collections. The AGENTS.md explicitly forbids Astro-specific content storage features.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Astro Content Collections | Using Astro's built-in content layer for research/team data — violates the "content portability is sacred" decision in AGENTS.md and 01-CONTEXT.md | blocking | Always read Markdown files with standard file I/O (import.meta.glob or fs + front-matter parser), never import from Astro content collections |
| Inline JSON data in components | Hardcoding data in .astro components makes content unmaintainable and non-portable | advisory | All content must come from src/data/*.json or src/content/*.md files |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>

Phase 2: Core Content Pages — Context gathered, ready for planning.

The discuss-phase workflow just completed for Phase 2. Four gray areas were discussed and resolved:
1. Content migration strategy (Team → Markdown, Research → Markdown + JSON, Publications → JSON)
2. Research-publication cross-linking (add `id` field to publications.json, `publicationIds` array in research frontmatter)
3. Team photos (representative stock/AI portraits)
4. Publication topic filters (keep current granular tags)

The 02-CONTEXT.md file has been written to `.planning/phases/02-core-content/02-CONTEXT.md`.

The DISCUSSION-LOG.md has NOT been written yet — it should be created before committing.

The next step in the GSD workflow is `/gsd-plan-phase 2` to create the execution plan.

</current_state>

<completed_work>

Completed Tasks:
- Task 1: Phase 2 context discussion with user — Done (all 4 gray areas resolved)
- Task 2: Write 02-CONTEXT.md with decisions — Done (written to `.planning/phases/02-core-content/02-CONTEXT.md`)
</completed_work>

<remaining_work>

- Task 3: Write 02-DISCUSSION-LOG.md — Not started
- Task 4: Create PLAN.md for Phase 2 — Not started (requires `/gsd-plan-phase 2`)
- Task 5: Execute Phase 2 plans — Not started (requires plan creation first)
</remaining_work>

<decisions_made>

- Decided to migrate team data entirely to Markdown (src/content/team/) and deprecate team.json for maximum portability and editability.
- Decided to migrate research themes to Markdown (src/content/research/) with YAML frontmatter for structured fields and prose body for descriptions, deprecating research.json.
- Decided to keep publications.json as JSON (already optimal tabular format), adding a dedicated `id` field to each entry for cross-linking.
- Chose explicit publication ID lookup via `publicationIds: []` array in research frontmatter over fuzzy title matching — more robust against title changes.
- Chose representative stock/AI photos for team members instead of SVG placeholders.
- Chose to keep the existing granular publication tag taxonomy (Breeding, Drought, HPLC, Statistics, etc.) instead of reorganizing into research-theme topics.
- Stuck with `import.meta.glob` or Node `fs` + front-matter parser for Markdown reading to preserve portability — no Astro Content Collections.
</decisions_made>

<blockers>
None
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-core-content/02-CONTEXT.md` — All Phase 2 implementation decisions. MUST read before planning or executing.
2. `.planning/phases/01-foundation/01-CONTEXT.md` — Prior phase decisions (design system locked, nav structure, font loading, content portability rules).
3. `.planning/ROADMAP.md` — Phase 2 goal, requirements (RES-01 to RES-03, TEAM-01 to TEAM-04, PUB-01 to PUB-04), success criteria.
4. `.planning/REQUIREMENTS.md` — Full v1 requirement definitions.
5. `AGENTS.md` — Project-specific guidance including the "no Astro Content Collections" rule.

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: Using Astro Content Collections or any Astro-specific content layer for storing data. This breaks portability. Structural mitigation: Only use standard Markdown + JSON files. Read them with standard file I/O (fs, front-matter parser, or import.meta.glob).
- ANTI-PATTERN: Hardcoding content in `.astro` component files. Structural mitigation: All content must live in `src/content/` (Markdown) or `src/data/` (JSON).

## Infrastructure State

- No running dev server or build processes.
- No external services or API keys needed for Phase 2 content.
- Static site built with `npm run build` (Astro + Tailwind).
- Content directories exist from Phase 1: `src/content/research/`, `src/content/team/` (contain READMEs, no content files yet).

## Pre-Execution Critique Required

- None — we are in context/discussion phase, not between design and execution.

<context>

The discuss-phase context for Phase 2 is complete and locked. The user was engaged and made clear decisions: full Markdown migration for team and research, JSON stays for publications, slug-based cross-linking, stock photos, and existing filter tags. No scope creep occurred. The user explicitly signaled they were ready for context write after all 4 areas were discussed.

The next step is creating the DISCUSSION-LOG.md, committing the context files, and then running `/gsd-plan-phase 2` to generate the execution plan.

</context>

<next_action>

1. Write 02-DISCUSSION-LOG.md in `.planning/phases/02-core-content/` documenting the 4 gray areas discussed and their resolutions.
2. Commit all Phase 2 context files together (02-CONTEXT.md, 02-DISCUSSION-LOG.md, .continue-here.md, HANDOFF.json).
3. Then proceed to `/gsd-plan-phase 2` to generate the PLAN.md for execution.

</next_action>
